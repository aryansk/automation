# Triage — cmu-sei/Polar #218

Date: 2026-08-06  
Repository: [cmu-sei/Polar](https://github.com/cmu-sei/Polar)  
Issue: [#218](https://github.com/cmu-sei/Polar/issues/218)  
PR: [#243](https://github.com/cmu-sei/Polar/pull/243)

## Selection gate

- The repository is publicly accessible and externally owned by `cmu-sei`.
- Issue #218 was open and unassigned, with `good first issue` and `security`
  labels.
- Live PR search found no PR for issue #218 before implementation.
- The issue has a narrow acceptance path: remove the implicit plaintext HTTP
  candidate from the default Docker credential lookup list while retaining
  HTTPS and the existing path variants.
- The repository's `license.txt` contains its MIT-style grant, although the
  GitHub license API reports `Other`.

## Implementation

Checkout: `/Users/aryansingh/Downloads/Projects/Automation/polar-218`  
Branch: `codex/issue-218-remove-http-registry-candidates`  
Commit: `e08287bb650cc51b0497eeeecf464956e270d606`

Changed `src/agents/resolver/src/main.rs` to remove the implicit
`http://<registry>` credential candidate, renamed the focused test to
`build_registry_candidates_excludes_http`, and removed two unused test imports.
The bare hostname, HTTPS, HTTPS-with-trailing-slash, and `/v1/` candidates are
unchanged.

## Validation evidence

Passed:

- `cargo test -p oci-resolver build_registry_candidates_excludes_http -- --nocapture`
  — 1 unit test and 1 integration test passed.
- `cargo test -p oci-resolver --bin oci-resolver -- --nocapture` — 2 tests passed.
- `cargo check -p oci-resolver`.
- `cargo clippy -p oci-resolver --bin oci-resolver --no-deps -- -D warnings`.
- `rustfmt --edition 2024 --check resolver/src/main.rs`.
- `git diff --check`.

Not passing as repository-wide gates:

- `cargo fmt --all -- --check` reports pre-existing formatting changes across
  unrelated files.
- `cargo clippy -p oci-resolver --all-targets -- -D warnings` reaches five
  existing diagnostics in `cassini/client/src/cli.rs` and `cassini/client/src/lib.rs`.

## Publication

- Fork: [aryansk/Polar](https://github.com/aryansk/Polar)
- Remote branch hash verified equal to local: `e08287bb650cc51b0497eeeecf464956e270d606`
- Draft PR #243 targets `cmu-sei/Polar:main` from
  `aryansk:codex/issue-218-remove-http-registry-candidates`.
- GitHub state: `OPEN`, draft, `mergeable=true`, `mergeable_state=clean`.
- Hosted checks: none reported.

## Counting state

PR #243 is open/draft and therefore does not count toward the 100 merged-PR
criterion. The live qualifying external merged count remains 2.

## Next action

Monitor PR #243 for maintainer feedback. Do not mark it countable until the
canonical upstream repository reports `MERGED` and the merge commit is recorded
in `PR_TRACKER.md`.
