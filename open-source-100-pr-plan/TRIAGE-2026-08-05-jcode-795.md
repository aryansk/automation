# Triage: 1jehuang/jcode issue #795

## Selection date

2026-08-05, Asia/Kolkata.

## Discovery and gates

- Source: [GitHub Trending, weekly](https://github.com/trending?since=weekly).
- Repository: [1jehuang/jcode](https://github.com/1jehuang/jcode), externally
  owned and MIT-licensed; default branch `master`; Rust codebase.
- Issue [#795](https://github.com/1jehuang/jcode/issues/795) was open,
  unassigned, and had no related PR at selection.
- Other live trending candidates were skipped when a duplicate PR already
  covered the issue, the repository had no usable license signal, or the scope
  required broad maintainer decisions.

## Issue scope

The OpenRouter/OpenAI-compatible provider slot shares transport and credential
state. Native OpenAI or another direct OpenAI-compatible endpoint could
therefore be presented as native OpenRouter in fast auth and route summaries,
especially after a stale process-global transport marker survived a runtime
switch.

## Implementation

- Checkout: `/Users/aryansingh/Downloads/Projects/Automation/jcode-795`
- Branch: `codex/issue-795-openrouter-identity`
- Commit: `67d35889a38bab8a4a04ae62c88d81fd02162e02`
- Added native OpenRouter credential detection based on configured endpoint
  provider-routing capability while preserving broad shared-slot detection for
  runtime startup.
- Updated fast auth and route classification to use native identity/runtime
  capability.
- Added regressions for native OpenAI, direct-compatible endpoints, genuine
  OpenRouter credentials, stale transport markers, and direct route behavior.

The commit includes DCO sign-off. The configured SSH signing key was
passphrase-protected and unavailable, so GitHub would not show a cryptographic
signature.

## Validation

- `cargo fmt --all -- --check`: passed.
- `cargo test -p jcode-base --lib openrouter -- --test-threads=1`: 30 passed.
- Full `jcode-base` library run: 1,236 passed, 1 ignored, 5 existing
  environment-sensitive `platform`, `session`, and `skill` failures; no
  changed test failed.
- `git diff --check`: passed.

## Publication blocker

- Fork: `https://github.com/aryansk/jcode`
- Branch was pushed and verified with `git ls-remote` at the commit above.
- GitHub CLI GraphQL PR creation returned
  `aryansk does not have the correct permissions to execute CreatePullRequest`.
- GitHub REST PR creation returned HTTP 404 for the same external-fork head.
- No upstream PR was created. This work is recorded as `EXT-022 BLOCKED`, is
  not in the merged-PR tracker, and contributes zero until a canonical upstream
  PR exists and is merged.

## Resume instructions

1. Do not create a duplicate PR for #795.
2. Revisit only if `1jehuang/jcode` grants a supported external contribution
   path or a maintainer asks for the pushed branch.
3. If publication becomes possible, verify the current `master` and issue/PR
   state again before opening exactly one draft PR.
