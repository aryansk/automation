# Triage — nodejs/node issue #55422

## Selection check

- Repository: [nodejs/node](https://github.com/nodejs/node)
- Issue: [#55422](https://github.com/nodejs/node/issues/55422)
- Title: ``Buffer.concat` and `Buffer.copy` silently produce invalid results
  when the operation involves indices equal or greater than 2^32``
- Checked: 2026-08-03
- Base branch: `main`
- State: open, unassigned, labeled `confirmed-bug`, `help wanted`, `buffer`,
  and `regression`
- Related open PR search: none at the final check

## Technical disposition

The issue has a reproducible large-buffer corruption case and a recent comment
reports that it still occurs on Node v24.11.0. The issue discussion points to
32-bit conversions in the native `SlowCopy`/buffer path, but the current `main`
implementation and test coverage must be checked before changing the integer
types or adding a regression. The work should stay limited to the Buffer copy
path and its tests.

## Project gate

Node's current `CONTRIBUTING.md` says an automation tool or bot not managed by
the project must seek explicit authorization by opening an issue with its owner
in `nodejs/admin` before interacting with `nodejs/node`. No such authorization
has been obtained in this task. Do not clone, edit, comment, or submit against
Node.js until that project-level gate is resolved.

## Resume action

After explicit Node.js automation authorization is verified, recheck #55422 and
open PRs, clone current `main`, reproduce the large-buffer behavior on the
supported platform, and inspect the existing native Buffer tests before making
the smallest safe fix.
