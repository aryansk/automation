# Triage — nodejs/node issue #40091

## Selection check

- Repository: [nodejs/node](https://github.com/nodejs/node)
- Issue: [#40091](https://github.com/nodejs/node/issues/40091)
- Title: `TextDecoder does not error incorrectly for legacy byte sequences`
- Checked: 2026-08-03
- Base branch: `main`
- State: open, unassigned, labeled `good first issue`, `encoding`, and `stale`
- Related open PR search: none at the final check

## Technical disposition

The issue reproduces a standards-compatibility defect in the Big5 decoder:
`new TextDecoder("Big5").decode(new Uint8Array([0x83, 0x5C]))` returns the
wrong code point instead of the WHATWG Encoding Standard behavior. The issue
also points to broader ICU/Encoding Standard compatibility concerns. This is
not a safe blind edit: it may involve ICU data or Node's encoding patches and
needs maintainer direction before implementation.

## Project gate

Node's current `CONTRIBUTING.md` says an automation tool or bot not managed by
the project must seek explicit authorization by opening an issue with its owner
in `nodejs/admin` before interacting with `nodejs/node`. No such authorization
has been obtained in this task. Do not clone, edit, comment, or submit against
Node.js until that project-level gate is resolved.

## Resume action

Keep #40091 as a gated candidate. First obtain or verify explicit Node.js
automation authorization and maintainer guidance on the ICU/WHATWG scope; then
recheck duplicates and reproduce the Big5 case on the current `main` branch.
