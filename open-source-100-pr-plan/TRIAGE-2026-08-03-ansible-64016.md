# Triage — ansible/ansible issue #64016

## Selection

- Repository: [ansible/ansible](https://github.com/ansible/ansible)
- Issue: [#64016](https://github.com/ansible/ansible/issues/64016)
- Title: `ansible 2.8.6 get_url repeatedly downloads file with force==no`
- Checked: 2026-08-03
- Base branch: `devel`
- Local checkout: `/Users/aryansingh/Downloads/Projects/Automation/ansible-64016`
- Branch: `codex/issue-64016-get-url-force`

## Live gates

- Issue state: open
- Assignees: none
- Labels: `easyfix`, `module`, `bug`, `affects_2.8`, `P3`
- Related open PR search: none at the final pre-publication check
- Base repository owner: Ansible, not Aryan
- Contribution instructions: `.github/CONTRIBUTING.md`; GitHub pull requests
  are accepted and the project requires DCO sign-off

## Maintainer direction

The issue discussion clarifies that `force: no` should skip downloading when
the destination exists. `force: yes` may retain conditional-download behavior,
and a checksum mismatch must still trigger a download. The implementation and
test stay within that scope.

## Verified-signature gate

- An Ansible bot comment on [PR #87345](https://github.com/ansible/ansible/pull/87345#issuecomment-5166550414)
  requires all commits to have verified signatures before the contribution can
  proceed.
- The initial three commits used DCO sign-offs via `git commit -s`, but DCO
  sign-off is not the same as a GitHub-verified commit signature.
- On 2026-08-05, GitHub CLI authentication was refreshed with the
  `admin:ssh_signing_key` scope. A dedicated passphrase-protected SSH signing
  key was registered, with its passphrase stored in macOS Keychain.
- The exact three-commit branch was re-signed and force-pushed with
  `--force-with-lease`. GitHub's API reports `verified: true` and `reason:
  valid` for all three replacement commits.
- The signature gate is resolved; the PR remains open/draft with the new
  hosted build pending and still does not count until merged.

## Change submitted

Initial commit `d998abc467693efea98923422d58bbfd20ebab71`, followed by
`456e5ca000966238a007051b92e5e864aff4a278` and
`96149f79311b8b6370f09ea0a226bf1cfc843379` after hosted CI review:

- Make `get_url` skip an existing destination when `force` is disabled and no
  checksum mismatch is present.
- Preserve the checksum-mismatch download path.
- Replace the old 304-based assertion with a regression request to an HTTP 500
  endpoint; the fixed code must exit before making that request.
- Keep the existing 304-status regression test as an explicit `force: yes`
  case, because it tests conditional-download metadata rather than the new
  default skip behavior.
- Rename that test to document its explicit force requirement after Azure build
  `187985` completed with 77 passes and 12 failures, all matrix fan-out of the
  same implicit-download assertion.
- Add a changelog fragment referencing #64016.

## Validation

- Ansible sanity `validate-modules`: passed under Python 3.14.
- Focused unit suite: 8 passed under Python 3.14.
- Python compilation of `get_url.py`: passed.
- YAML parsing of the integration task and changelog fragment: passed.
- `git diff --check`: passed.
- Hosted CI's first integration run reached the `get_url` target but failed at
  the pre-existing 304-status assertion because the corrected default behavior
  skipped the existing destination before producing `status_code`. The
  explicit `force: yes` follow-up was pushed as `456e5ca000`; Azure build
  `187985` then completed with 77 passes and 12 failures from that same test's
  implicit-download assumption. The test-only correction is now pushed as
  `96149f7931`, and Azure build `187986` completed with 77 passes and 12
  checksum-algorithm connection failures.
- Local validation after `96149f7931`: Ansible sanity `validate-modules`, the
  focused 8-test Python 3.14 unit suite, Python compilation, YAML parsing, and
  `git diff --check` all pass.
- Hosted Azure build `187986` completed with 77 passing checks and 12 failures.
  The failed matrix legs reported connection resets or a remote end closing the
  connection in the pre-existing checksum-algorithm task
  `test/integration/targets/get_url/tasks/hashlib.yml:11`; the changed
  no-download and explicit-force assertions were not the reported failure.
- Re-signing changed commit metadata and signatures only: the final tree is
  identical to `96149f79311b8b6370f09ea0a226bf1cfc843379`, and local Git
  verifies all three replacement commits as `Good "git" signature`.
- The replacement commit hashes are `0ee39a808b9696d6a8c123ba982bd211fc9d0cf5`,
  `d76b2d54d6ae64b8dcb361b6e149564b48958ee1`, and
  `64300db98b0ab9ad875e4c4ce9a6b8cc3ba38dc4`.
- Integration target: not run locally; Ansible marks it as container-backed
  (`cloud/httptester`) and Docker is unavailable in this environment.

## Publication

- Fork: [aryansk/ansible](https://github.com/aryansk/ansible)
- Remote branch: `codex/issue-64016-get-url-force`
- Draft PR: [ansible/ansible#87345](https://github.com/ansible/ansible/pull/87345)
- PR state at handoff: open, draft, base `devel`; the verified-signature gate
  is resolved on the replacement commits; Azure build `187985` is
  completed/failed with 77 passes and 12 failures from the old implicit-download
  assertion, build `187986` for `96149f7931` is completed/failed with 77
  passes and 12 checksum-algorithm connection failures, and build `188137` for
  the signed head is currently 76 successful, 11 in progress, and 1 queued
- Local and fork branch hashes match:
  `64300db98b0ab9ad875e4c4ce9a6b8cc3ba38dc4`

## Resume action

Check PR #87345's new build `188137` and maintainer feedback. Treat build
`187986` as an environment/integration-server failure unless a later log ties a
failure to the changed assertions. Respond only within the `get_url`
force/checksum behavior and its regression coverage. Do not count this
contribution until the canonical PR is merged.
