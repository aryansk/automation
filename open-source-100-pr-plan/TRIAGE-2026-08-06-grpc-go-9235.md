# Triage — grpc-go #9235

## Live selection gate — 2026-08-06

- Issue [#9235](https://github.com/grpc/grpc-go/issues/9235), “Add support for
  Authority in stats.Handler,” is open.
- The current issue-specific PR search found no competing implementation PR.
- Maintainer Easwar Swaminathan said the field addition is welcome for review
  and specifically required using `callHdr.Host`, not `callHdr.Authority`,
  because the latter is only populated for overrides.
- `grpc/grpc-go` is public, externally owned, on `master`, and reports the
  Apache-2.0 license through GitHub.

## Scope

Expose the effective client HTTP/2 authority through `stats.OutHeader` and
populate it from the host value used by the client transport. The test fixture
adds an authority call option and verifies the effective host in the emitted
out-header stats event.

## Local implementation

- Checkout: `/Users/aryansingh/Downloads/Projects/Automation/grpc-go-9235`
- Branch: `codex/issue-9235-stats-authority`
- Commits: `3ffedf3` — `stats: expose client authority in OutHeader`, followed
  by `df0c780` — `gofmt stats authority regression test`
- Validation: staged and post-commit `git diff --check` passed. The changed Go
  files are formatted, and `mise exec go@1.26.5 -- go test ./stats
  ./internal/transport` passes; the pinned toolchain also removes the earlier
  local Go-availability limitation.

## Publication state

Created the `aryansk/grpc-go` fork, pushed `codex/issue-9235-stats-authority`,
verified the fork branch hash matches
`df0c7800e6b73c8e5c57ba567face53a762491b2`, and opened draft [PR #9296](https://github.com/grpc/grpc-go/pull/9296)
against `master`. It is authored by `aryansk`; focused tests pass through Go
1.26.5 via mise. EasyCLA reports missing CLA authorization. Do not sign any
legal agreement automatically; the PR remains uncounted until canonical
upstream merge. Refined-github #9941 is excluded and does not govern this
independent repository.
