# Triage: `rtk-ai/rtk#3448`

## Selection evidence

- Repository: [rtk-ai/rtk](https://github.com/rtk-ai/rtk)
- Issue: [#3448](https://github.com/rtk-ai/rtk/issues/3448)
- The issue is open, externally owned, and labeled `documentation`, `good first
  issue`, `priority:medium`, and `area:docs`. It identifies two exact stale
  claims: `rtk gh api` is documented as saving 26%, while the implementation
  intentionally passes the full JSON response through at 0% savings.
- No competing open PR referencing #3448 was found at selection. The issue
  supplies the affected files and a source-side `--jq` filtering direction.
- The repository is Apache-2.0 licensed, uses `develop` as its default branch,
  and its [contribution guide](https://github.com/rtk-ai/rtk/blob/develop/CONTRIBUTING.md)
  explicitly welcomes documentation improvements and uses Conventional
  Commits. Recent merged changes show active maintainer review cadence.

## Implementation

- Checkout: `/Users/aryansingh/Downloads/Projects/Automation/rtk-3448`
- Branch: `codex/issue-3448-docs`
- Commit: `031626014ad6579d8886ef5e23b874fd2752f65b`
- Updated the generated `rtk init` instruction block to show `gh api` as a
  deliberate 0%-savings passthrough, corrected the GitHub range/table, and
  added a `--jq` example.
- Updated `docs/usage/FEATURES.md` to describe the same behavior in its GitHub
  CLI table.
- Added a regression test that asserts the passthrough guidance remains and
  the old 26% wording cannot return.

## Validation

- `cargo fmt --all -- --check` passed.
- `cargo test --bin rtk test_init_describes_gh_api_as_passthrough` passed.
- `cargo test --bin rtk` passed: 2,563 tests passed, 8 ignored, 0 failed.
- `git diff --check` passed.

## Publication state

An external contributor opened [PR #3450](https://github.com/rtk-ai/rtk/pull/3450)
after the local implementation was completed. It changes the same two files,
addresses issue #3448, and is currently open and non-draft. No `aryansk/rtk`
fork or PR was created; the local contribution is not merged or countable and
must not be submitted as a duplicate.

## Next action

Monitor [PR #3450](https://github.com/rtk-ai/rtk/pull/3450). If it closes
without resolving issue #3448, re-check the live issue and PR state before
considering any distinct follow-up; do not create a duplicate.
